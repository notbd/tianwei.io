import type { Theme } from '@/lib/types/themeTypes'

// ---- CONFIGURATION CONSTANTS ----

// default color theme
export const DEFAULT_THEME: Theme = 'system'

// ---- KEY CONSTANTS ----

export const KEYS = {
  // cookie key for last chosen theme
  LAST_CHOSEN_THEME: 'last-chosen-theme',
}
